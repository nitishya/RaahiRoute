steps:
  # Build Backend Image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/raahi-route-backend', './backend']
    id: 'build-backend'

  # Build Frontend Image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/raahi-route-frontend', './frontend']
    id: 'build-frontend'

images:
  - 'gcr.io/$PROJECT_ID/raahi-route-backend'
  - 'gcr.io/$PROJECT_ID/raahi-route-frontend'
